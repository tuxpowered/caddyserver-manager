name: caddy_manager_mobile
description: A new Flutter project for managing Caddy Server.
version: 1.0.0+1
publish_to: 'none'

environment:
  sdk: '>=3.5.3 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  http: ^1.1.0
  shared_preferences: ^2.2.2
  google_mobile_ads: ^5.2.0
  provider: ^6.1.2
  url_launcher: ^6.2.1
  local_auth: ^2.1.6
  flutter_secure_storage: ^9.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true
